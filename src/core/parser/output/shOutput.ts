import ApiWrapper from "../../types/api/apiWrapper";
import { RSegment } from "../../types/parser/rSegment";

export default function shOutput(
    segments: RSegment.SegmentInterface,
    context: {
        api: ApiWrapper
    }) {
    let script = "";

    return script;
}